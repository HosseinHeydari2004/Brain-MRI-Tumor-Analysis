# --- Deep learning ---
# For a CUDA build matching your GPU/driver, install torch/torchvision from
# https://pytorch.org/get-started/locally/ BEFORE running `pip install -r requirements.txt`
# (pip will otherwise resolve the CPU-only build).
torch>=2.2,<3.0
torchvision>=0.17,<1.0

# --- Web apps ---
streamlit>=1.32
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6

# --- Imaging / vision ---
pillow>=10.0
opencv-python-headless>=4.9
numpy>=1.26,<3.0
matplotlib>=3.8

# --- Data ---
kagglehub>=0.3

# --- Dev / testing (optional) ---
httpx>=0.27
